/**
 * Entrypoint for Vercel
 * This file is created to satisfy Vercel's search for src/index.ts
 * even though we are using the api/ directory for serverless functions.
 */
import { appRouter } from './api';

export { appRouter };
