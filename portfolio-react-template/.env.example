# Portfolio React Template - Environment Variables
# Copy this file to .env and fill in your values

# API Configuration
# URL for the backend API (tRPC endpoint)
# Default: http://localhost:3000/trpc (for local development)
# Production: Set this to your deployed backend URL
VITE_API_URL=http://localhost:3000/trpc

# Mock API Mode (for development/testing without backend)
# Set to 'true' to use mock RAG responses instead of real backend
# Useful for frontend development before Epic 4 (Backend) is complete
# Default: false (use real backend)
VITE_USE_MOCK_API=false

# Contact Information
# Email address displayed in error messages as fallback contact
# Used by the lead generation chat component
# Default: design@vansh.fyi
VITE_CONTACT_EMAIL=design@vansh.fyi

# Note: All Vite environment variables must be prefixed with VITE_
# to be exposed to the client-side code
# See: https://vitejs.dev/guide/env-and-mode.html
